<homepage>

	<!-- list of all topics -->
	<div>
		<ul>
			<li>
				<button></button>
			</li>
		</ul>
	</div>

	<!-- displayed topic -->
	<div>
		<article/>
	</div>

	<script>
		// riot.route(function(collection,id,action) {
		// 	$.get(collection+'/'+id){}.done(function(data){riot.update(data);})});
	</script>

</homepage>